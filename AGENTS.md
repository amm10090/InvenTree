# AGENTS.md（项目级记忆与待办协议）

本文件用于当前自定义 work tree 的长期协作约定。目标是把需求、执行、验收和长期记忆串成一条可追溯链路，避免遗漏和重复沟通。

每次在这个 work tree 里提出新需求，先登记待办，再开始执行。登记位置在 `PLANS.md` 的 `TODO` 区，必须使用可勾选任务格式。任务至少写清需求日期、目标、验收标准、涉及文件路径。

任务进入执行状态后，要把条目移动到 `IN_PROGRESS`，并在任务下持续补充关键进展和验证结果。完成后必须勾选为 `[x]`，写明完成日期、关联提交（如有）和最终验证方式，然后移动到 `DONE`。

凡是对后续开发有长期价值的内容，都要同步沉淀到 `MEMORIES.md`，包括自定义改动、关键决策、约定变更、已踩坑与修复结论。`MEMORIES.md` 只记录稳定且可复用的信息，不写临时过程噪音。

`PLANS.md` 负责阶段性任务追踪，`MEMORIES.md` 负责长期知识沉淀。两份文件都属于项目资产，后续需求必须持续维护，不允许只改代码不更新记录。
